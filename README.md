# ME VS ME

A personal training, nutrition, body-composition and progress tracker for Graham and Louise.

The app runs as a standalone browser-based application and stores each profile locally on the device. Use the in-app backup before changing device, clearing browser data or moving to a new version.

Open `index.html` to use the app.
